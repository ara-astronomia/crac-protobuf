syntax = "proto3";
package crac_protobuf;

enum MessageType {
  UNKNOWN = 0;
  ROOF = 1;
  BUTTONS = 2;
  TELESCOPE = 3;
  CHART = 4;
  CURTAINS = 5;
}

message Envelope {
  MessageType type = 1;
  bytes payload = 2;  // Qui ci va il messaggio serializzato (RoofResponse, ecc.)
}